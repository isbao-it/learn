# 📚 爆款心法 · 技术概念总览
> 级别一 3~8：Pydantic / SQLAlchemy / ChromaDB / LangChain / LangGraph / Streamlit + 阶段7：FastAPI
> 先看概念，再看案例。每个技术 = 是什么 → 解决什么 → 核心概念 → 重要用法 → 项目位置

---

## 1. Pydantic —— 数据门卫

**是什么**：Python 数据验证库，用类型注解定义数据结构。
**解决什么**：脏数据在源头被拦截；JSON 与 Python 对象自动互转。

### 核心概念（主体）
| 概念              | 说明                                        |
| --------------- | ----------------------------------------- |
| BaseModel       | 定义一个模型类（字段 + 类型）                          |
| 类型注解            | str / int / date / Enum / list[str] 等     |
| Field 约束        | min_length、max_length、ge（>=）、le（<=）       |
| field_validator | 自定义校验逻辑（比如"文案不能是空白"）                      |
| 自动转换            | "2026-07-01" → date 对象；JSON 文本 → 模型       |
| 序列化             | model_dump() 转字典，model_dump_json() 转 JSON |

### 重要用法（骨架）
```python
from pydantic import BaseModel, Field, field_validator

class VideoCase(BaseModel):
    title: str = Field(min_length=1, max_length=100)
    views: int = Field(ge=0)                 # 播放量不能为负

    @field_validator("content")
    @classmethod
    def not_blank(cls, v: str) -> str:
        if not v.strip():
            raise ValueError("文案不能是空白")
        return v.strip()

case = VideoCase(title="...", views=100)     # 非法数据在此抛错
print(case.model_dump_json())                # 输出 JSON
```

### 项目位置
任务卡 02（案例数据模型）、任务卡 10（API 请求/响应格式）、所有 LLM 结构化输出的解析。

---

## 2. SQLAlchemy —— 数据库翻译官

**是什么**：Python ORM（对象关系映射），用 Python 对象操作数据库。
**解决什么**：不用手写 SQL；表结构用 Python 类定义；对象 ↔ 数据库行互转；数据永久保存。

### 核心概念（主体）
| 概念                     | 说明                             |
| ---------------------- | ------------------------------ |
| create_engine          | 连接数据库（sqlite:///xxx.db 就是本地文件） |
| DeclarativeBase        | 声明式基类，模型类都继承它                  |
| Mapped / mapped_column | 定义列（类型、主键、默认值）                 |
| Session                | 会话 = 所有增删改查的入口                 |
| commit / rollback      | 提交才真正写入；出错回滚                   |
| select()               | 查询（2.0 现代风格，链式条件+排序）           |

### 重要用法（骨架）
```python
from sqlalchemy import create_engine, select
from sqlalchemy.orm import Session, DeclarativeBase, Mapped, mapped_column

engine = create_engine("sqlite:///cases.db")

class Base(DeclarativeBase): pass

class VideoCase(Base):
    __tablename__ = "video_cases"
    id: Mapped[int] = mapped_column(primary_key=True)
    title: Mapped[str]

Base.metadata.create_all(engine)             # 建表

with Session(engine) as s:
    s.add(VideoCase(title="..."))            # 增
    s.commit()                               # ⚠️ 不 commit 不保存！
    rows = s.scalars(select(VideoCase).order_by(VideoCase.views.desc())).all()  # 查
    one = s.get(VideoCase, 1)                # 按主键取
    s.delete(one); s.commit()                # 删
```
`with` 语句是 Python 中用于**资源管理**的语法结构，它的核心作用是确保代码在执行完毕后，能够自动、安全地清理和释放所占用的资源，无论中间是否发生了异常。

### 项目位置
任务卡 02（案例表 + 分析记录表）；后期升级 PostgreSQL 只需换连接串。

---

## 3. ChromaDB —— 向量数据库（搜"意思"）

**是什么**：存"向量"的数据库。向量 = 文本的数字表示（embedding）。
**解决什么**：关键词搜不到同义表达（搜"焦虑"搜不到"内耗"）；按语义相似度检索。

### 核心概念（主体）
| 概念 | 说明 |
|------|------|
| Embedding | 文本 → 一串数字，语义相近的文本数字也相近 |
| PersistentClient | 持久化客户端（数据存本地目录） |
| Collection | 向量集合（≈ 数据库的表） |
| add() | 写入：ids + documents（文本）+ metadatas（附加字段） |
| query() | 查询：给一段文本，返回最相似的 N 条 |
| n_results | 返回条数 |

### 重要用法（骨架）
```python
import chromadb

client = chromadb.PersistentClient(path="./chroma_data")
col = client.get_or_create_collection("cases")

col.add(ids=["1", "2"],
        documents=["三个原因拆穿你的焦虑本质", "月薪3千和3万的区别"],
        metadatas=[{"title": "焦虑视频"}, {"title": "职场视频"}])

results = col.query(query_texts=["内耗怎么办"], n_results=2)
print(results["documents"])   # 返回语义最接近的文案
```

### 项目位置
任务卡 05 三重混合检索里的 **Dense（语义）检索** 支路。

> ⚠️ **选型铁律（实战验证）**：embedding 模型决定检索质量，必须按语言选型！
> - chromadb 默认的 `all-MiniLM-L6-v2` 是**英文模型**，处理中文会错乱（搜"焦虑"命中"存钱"）
> - 中文场景必须用 `BAAI/bge-small-zh-v1.5`（中文向量模型，BAAI 出品）
> - 实测对比：英文模型 3 个查询错 2 个；中文模型 3 个查询全对
> - 爆款心法项目 = 纯中文场景，生产必须用 bge 系列中文模型

---

## 4. LangChain —— LLM 应用框架

**是什么**：封装大模型调用的框架：模型、提示词、输出解析、链式组合。
**解决什么**：不用手写 HTTP 调用；Prompt 模板复用；LLM 输出转成结构化数据。

### 核心概念（主体）
| 概念 | 说明 |
|------|------|
| ChatOpenAI | 模型封装（DeepSeek 兼容 OpenAI 协议，改 base_url 即可） |
| PromptTemplate | 提示词模板，{变量} 占位 |
| 消息角色 | System（系统设定）/ Human（用户）/ AI（助手） |
| OutputParser | 解析输出：StrOutputParser（纯文本）/ PydanticOutputParser（JSON 转模型） |
| LCEL 管道 | 用 | 把 模板→模型→解析器 串成一条链 |
| chain.invoke() | 执行链条 |

### 重要用法（骨架）
```python
from langchain_openai import ChatOpenAI
from langchain_core.prompts import PromptTemplate
from langchain_core.output_parsers import StrOutputParser

llm = ChatOpenAI(
    base_url="https://api.deepseek.com",
    api_key="sk-xxx",
    model="deepseek-chat",
    temperature=0.3,
)

prompt = PromptTemplate.from_template(
    "你是爆款视频分析师。分析这段文案的心理机制：
{text}"
)

chain = prompt | llm | StrOutputParser()     # LCEL 链
result = chain.invoke({"text": "视频文案……"})
print(result)
```

### 项目位置
所有 Agent（任务卡 03~08）的 LLM 调用底座；PydanticOutputParser 实现"返回结构化 JSON"。

> ⚠️ **实战两个坑（DeepSeek + LangChain）**：
> 1. **OUTPUT_PARSING_FAILURE**：LLM 可能返回带 `properties` 包裹的 JSON（把 Schema 当结构），解析器会当场报错——修复：prompt 明确"直接输出 JSON 对象本身"
> 2. **DeepSeek JSON 模式**：用 `model_kwargs={"response_format": {"type": "json_object"}}` 时，**prompt 必须包含 "json" 字样**，否则 API 报 400
> 3. 需要文本输出和 JSON 输出并存时，用两个 llm 实例（别全局开 JSON 模式）

---

## 5. LangGraph —— 多 Agent 编排

**是什么**：把多个步骤/Agent 编排成"状态图"：节点（干活的函数）+ 边（流转规则）。
**解决什么**：多 Agent 协作流水线；条件分支（比如要不要走某条路）；步骤间共享状态。

### 核心概念（主体）
| 概念 | 说明 |
|------|------|
| StateGraph | 状态图（整个流水线的蓝图） |
| State | 共享状态（TypedDict，节点读写） |
| Node | 节点：一个函数，输入 state，返回更新的字段 |
| Edge | 边：A 完成后走 B |
| conditional edge | 条件边：根据结果决定走哪条路 |
| 编译 | graph.compile() → app.invoke(初始状态) |

### 重要用法（骨架）
```python
from typing import TypedDict
from langgraph.graph import StateGraph

class State(TypedDict):
    text: str
    segments: list
    psychology: list

def deconstruct(state):      # 节点1：拆叙事结构
    return {"segments": ["开头", "冲突", "结尾"]}

def identify(state):         # 节点2：识别心理机制
    return {"psychology": ["损失厌恶", "身份认同"]}

g = StateGraph(State)
g.add_node("deconstruct", deconstruct)
g.add_node("identify", identify)
g.add_edge("deconstruct", "identify")   # 先拆解，再识别
g.set_entry_point("deconstruct")
g.set_finish_point("identify")

app = g.compile()
result = app.invoke({"text": "文案……", "segments": [], "psychology": []})
```

### 项目位置
任务卡 03→04→07 的多 Agent 流水线编排（拆解 → 心理识别 → 关联案例）。

---

## 6. Streamlit —— 快速前端

**是什么**：纯 Python 写网页界面的框架，不用 HTML/JS/CSS。
**解决什么**：几十行 Python 出一个可交互的分析页面；快速演示/内部工具。

### 核心概念（主体）
| 概念 | 说明 |
|------|------|
| 脚本即页面 | 代码从上到下执行，每次交互整页重跑 |
| st.title / st.write | 文本输出 |
| st.text_input / st.text_area | 输入框 |
| st.button / st.selectbox | 按钮 / 下拉选择 |
| st.dataframe / st.metric | 表格 / 指标卡片 |
| st.columns / st.sidebar | 布局：多列 / 侧边栏 |
| st.session_state | 跨交互保存变量（重跑不清空） |

### 重要用法（骨架）
```python
import streamlit as st

st.title("🔥 爆款心法分析")
text = st.text_area("粘贴视频文案", height=200)

if st.button("开始分析"):
    st.write("分析中……")
    st.metric("检测到的心理机制", 3)
    st.success("分析完成")
```

### 项目位置
任务卡 11：页面1 心理分析、页面2 案例检索、页面3 对标分析。

---

## 7. FastAPI —— API 服务框架

**是什么**：Python 的 Web 框架，把功能包成 REST API 接口。
**解决什么**：前端/其他程序通过网址调用你的 AI 功能，返回 JSON；前后端分离、多端访问、可部署。

### 核心概念（主体）
| 概念 | 说明 |
|------|------|
| REST API | URL = 接口地址，POST/GET = 动作，JSON = 数据格式特定是接口清晰，看一眼 URL 和方法就知道在干什么|
| @app.post("/analyze") | 装饰器定义接口（URL + 方法） |
| 请求/响应模型 | Pydantic 定义，自动验证（脏数据返回 422） |
| uvicorn | ASGI 服务器，负责运行 FastAPI 应用 |
| /docs | 自动生成的交互式 API 文档（Swagger UI） |
| CORS 中间件 | 允许前端跨端口调用（Streamlit 前端必备） |
| HTTPException | 返回友好错误码（如 LLM 挂了返回 502） |

### 补充
- **FastAPI**：依赖注入、自动生成 API 文档、异步处理
- **uvicorn**：ASGI 服务器，支持异步请求，性能强于 WSGI 服务器（如 gunicorn）

### 重要用法（骨架）
```python
from fastapi import FastAPI
from pydantic import BaseModel

app = FastAPI(title="爆款心法 API")

class AnalyzeRequest(BaseModel):
    text: str

class AnalyzeResponse(BaseModel):
    segments: list[str]
    psychology: list[str]
    report: str

@app.post("/analyze", response_model=AnalyzeResponse)
def analyze(req: AnalyzeRequest):
    # 在这里调用 LangGraph 流水线
    return AnalyzeResponse(segments=[...], psychology=[...], report="...")

@app.get("/health")
def health():
    return {"status": "ok"}
```
- **FastAPI**：依赖注入、自动生成 API 文档、异步处理
- **uvicorn**：ASGI 异步服务器接口，性能强于 WSGI 服务器同步请求（如 gunicorn）

### 重要用法（骨架）
```python
from fastapi import FastAPI
from pydantic import BaseModel

app = FastAPI(title="爆款心法 API")

class AnalyzeRequest(BaseModel):
    text: str

class AnalyzeResponse(BaseModel):
    segments: list[str]

### 重要用法（骨架）
```python
from fastapi import FastAPI
from pydantic import BaseModel

app = FastAPI(title="爆款心法 API")

class AnalyzeRequest(BaseModel):
    text: str

class AnalyzeResponse(BaseModel):
    segments: list[str]
    psychology: list[str]
    report: str

@app.post("/analyze", response_model=AnalyzeResponse)
def analyze(req: AnalyzeRequest):
    # 在这里调用 LangGraph 流水线
    return AnalyzeResponse(segments=[...], psychology=[...], report="...")

@app.get("/health")
def health():
    return {"status": "ok"}
```
启动命令：`uvicorn main:app --port 8000`（浏览器打开 /docs 即可交互测试）

### 项目位置
任务卡 10（API 核心路由）：/analyze、/compare、/cases/search 全是这个模板复制出来的。

> ⚠️ **工程化三件套（实战验证）**：
> 1. **日志**：logging 记录每次请求，排查问题用
> 2. **CORS**：allow_origins 生产环境必须限定具体域名（不要用 *）
> 3. **错误处理**：LLM 异常 → 502 友好提示（不要 500 裸奔）
> 下一步：Docker 容器化部署（任务卡 12，Dockerfile + docker-compose 一键启动）

## 8. Docker —— 容器化部署

## 是什么
把你的应用 + 所有依赖打包成一个"集装箱"（镜像），在任何机器上一键运行。

## 解决什么问题
- "我电脑上能跑啊？" → 镜像所到之处环境 100% 一致
- 换机器/答辩/上云 → `docker compose up` 一条命令复活整个项目

## 核心概念
| 概念 | 类比 | 说明 |
|------|------|------|
| 镜像 Image | 菜谱 | 只读模板：代码+Python+全部依赖 |
| 容器 Container | 炒出来的菜 | 镜像运行的实例，隔离、秒级启停 |
| Dockerfile | 菜谱写法 | 描述怎么打包的文本文件 |
| docker-compose | 整桌菜单 | 一条命令编排多个服务（API+数据库） |
| 卷 Volume | 保险箱 | 容器删了数据还在（数据库必配） |

## 重要用法（背这个骨架）
```dockerfile
FROM python:3.11-slim        # 基础镜像
WORKDIR /app                 # 容器工作目录
COPY requirements.txt .      # 先复制依赖清单（利用缓存层）
RUN pip install -r requirements.txt  # 装依赖（最慢的一步）
COPY . .                     # 再复制代码（改代码不触发重装依赖）
EXPOSE 8000                  # 声明端口
CMD ["python", "-m", "uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```
```yaml
# docker-compose.yml 核心四项
services:
  api:
    build: .           # 用当前目录 Dockerfile 构建
    ports: ["8000:8000"]  # 宿主机端口:容器端口
    env_file: .env     # 注入密钥（不要把 key 写进镜像！）
    restart: unless-stopped
```

## 常用命令（就这几个）
```bash
docker compose build     # 构建镜像
docker compose up -d     # 后台启动容器
docker ps                # 看运行中的容器
docker logs 容器名       # 看容器日志（排查问题第一站）
docker compose down      # 停止并删除容器（镜像还在）
```

## 实战踩坑记录（今天真实遇到的）
1. **流氓软件锁文件**：AlibabaProtect 锁定 Docker 的 kernel 文件导致安装失败。
   正解：安全模式下删除（bcdedit /set safeboot minimal → 重启删目录 → 恢复正常启动）
2. **PATH 未刷新**：装完 Docker 老终端找不到 docker / docker-credential-desktop，
   构建报 "executable file not found in %PATH%"。正解：重开终端（或临时 export PATH）
3. **中文 JSON 过 curl 损坏**：Windows bash 编码问题，用 `--data @文件` 传 UTF-8 文件
4. **.dockerignore 必写**：不排除 .venv 会把几百 MB 本地虚拟环境传进构建

## 在爆款心法里的位置
任务卡 12：项目根目录写 Dockerfile + docker-compose.yml，
把 API(任务卡10) + PostgreSQL(任务卡01) + 前端(任务卡11) 编排成一个 compose 文件，
答辩演示 = 换台电脑一条命令跑起来。

## 练习位置
`C:\work\learn\07_fastapi\`：Dockerfile / docker-compose.yml / .dockerignore 已跑通

---

## 🗺️ 技术关系图（它们怎么协作）

```
Docker(8) 容器化：整套服务打包，一条命令在任何机器跑起来
   │
   ▼
用户粘贴文案
   │
   ▼
Streamlit(6) 前端 ──调用──► FastAPI(7) 后端
                            │
        ┌───────────────────┼───────────────────┐
        ▼                   ▼                   ▼
   Pydantic 验证      LangGraph 编排        SQLAlchemy 存取
   请求/响应数据       多 Agent 流水线       案例库(SQLite)
                            │
                    LangChain 调 LLM
                            │
                    ChromaDB 向量检索
```

**记忆口诀**：Pydantic 守门 → SQLAlchemy 存数 → ChromaDB 搜义 → LangChain 对话 → LangGraph 编排 → Streamlit 展示 → FastAPI 发布 → Docker 上线。

---
